#include<vector>
#include<algorithm>
using namespace std;

class Solution {
public:
    int trap(vector<int>& height) {
			int total_water = 0;
			const int N = height.size();
			if(N<3)
				return 0;

			return total_water;
    }
};